# Azure AD and MSAL.js demo
Small presentation demo to show how to authenticate an app with Azure AD and MSAL.js. After cloning, run:

```bash
yarn install
node server
```

Then navigate to `https://localhost:1337` to view the application.
